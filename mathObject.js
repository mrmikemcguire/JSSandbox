const num1 = 100;
const num2 = 50;
let val;

val = Math.PI;

//just type Math. to see the built-in functions
val = Math.
val = Math.round(2.5);
val = Math.ceil(2.5);
val = Math.floor(2.5);
val = Math.sqrt(64);
val = Math.max(23, 14, 67, 53);
val = Math.min(23, 14, 67, 53);
val = Math.floor(Math.random() * 5 + 1);
console.log(val);
